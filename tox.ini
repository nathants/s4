[tox]
envlist = py37

[testenv]
usedevelop = True
passenv = *
install_command =
    pip install --process-dependency-links {opts} {packages}
commands =
    py.test -svx --tb native --boxed tests/
deps =
    pytest
    pytest-xdist
